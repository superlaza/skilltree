React = require 'react'

Course = React.createClass
	componentDidMount: ->

	componentDidUpdate: ->
	

	render: ->
		
		# <button onClick={addSemester}> add semester</button>
		<div class='course'>
			um
		</div>


mapStateToProps = (state) ->
	state: state.toJS()

module.exports =
	Course: Course